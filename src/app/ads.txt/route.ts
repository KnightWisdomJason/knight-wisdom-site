export function GET() { const publisherId = process.env.ADSENSE_PUBLISHER_ID; const body = publisherId ? `google.com, ${publisherId}, DIRECT, f08c47fec0942fa0\n` : "# Add ADSENSE_PUBLISHER_ID to publish your authorized Google AdSense seller record.\n"; return new Response(body, { headers: { "content-type": "text/plain; charset=utf-8", "cache-control": "public, max-age=3600" } }); }
